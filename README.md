to install script global:

```
npm i - global
```

to run script:

```
git-init
git-init --repo-name [REPO_NAME]
git-init --no-initial-commit
```

By default it create `public` repository on GitHub with the same name as used for folder name.
